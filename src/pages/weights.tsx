import { Box, Heading } from "@chakra-ui/layout";
import { RouteComponentProps } from "@reach/router";
import * as React from "react";

export default function Weights(props: RouteComponentProps) {
  return (
    <Box>
      <Heading>Ver pesos</Heading>
    </Box>
  );
}
