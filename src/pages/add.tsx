import { Box, Heading } from "@chakra-ui/layout";
import { RouteComponentProps } from "@reach/router";
import * as React from "react";

export default function Add(props: RouteComponentProps) {
  return (
    <Box>
      <Heading>Agregar Peso</Heading>
    </Box>
  );
}
